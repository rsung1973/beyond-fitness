@using System.IO
@using System.Linq.Expressions

@using WebHome.Helper
@using WebHome.Models.Locale
@using WebHome.Models.ViewModel
@using WebHome.Models.DataEntity
@using WebHome.Controllers
@using Newtonsoft.Json
@{

    ModelStateDictionary _modelState;
    ModelSource<UserProfile> models;
    TrainingItem _model;

    _modelState = (ModelStateDictionary)ViewBag.ModelState;
    models = ((SampleController<UserProfile>)ViewContext.Controller).DataSource;
    _model = (TrainingItem)this.Model;
    TrainingItemViewModel _viewModel = (TrainingItemViewModel)ViewBag.ViewModel;
    TrainingStage _stage = (TrainingStage)ViewBag.TrainingStage;
}
<div class="row clearfix">
    <div class="col-sm-6 col-12">
        <div class="input-group">
            <input type="text" class="form-control form-control-danger" placeholder="休息時間" value="@_viewModel.BreakInterval" name="BreakInterval" />
            <span class="input-group-addon font-10">
                秒
            </span>
        </div>
    </div>
    <div class="col-sm-6 col-12">
        <div class="input-group">
            <input type="text" class="form-control" placeholder="組數" value="@(_viewModel.Repeats ?? "1")" name="Repeats" />
            <span class="input-group-addon font-10">
                組
            </span>
        </div>
    </div>
    <div class="col-12">
        <div class="form-group">
            <textarea rows="2" class="form-control no-resize" placeholder="其他更多說明" name="Remark">@(_viewModel.Remark)</textarea>
        </div>
    </div>
</div>
<div class="row clearfix">
    <div class="col-lg-12">
        <button type="button" class="btn btn-danger btn-round btn-simple btn-round waves-effect waves-red btnDelete">刪除，不想要了</button>
        <button type="button" class="btn btn-darkteal btn-round waves-effect float-right btnConfirm">確定，不後悔</button>
    </div>
</div>
