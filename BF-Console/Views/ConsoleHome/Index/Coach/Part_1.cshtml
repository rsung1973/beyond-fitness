

@using System.IO
@using System.Linq.Expressions

@using WebHome.Helper
@using WebHome.Models.Locale
@using WebHome.Models.ViewModel
@using WebHome.Models.DataEntity
@using WebHome.Controllers
@using Newtonsoft.Json

@{
    Layout = "~/Views/ConsoleHome/Index/Coach/Part_1_tempate.cshtml";
    ModelSource<UserProfile> models;
    ModelStateDictionary _modelState;
    UserProfile _model;
    LessonTimeBookingViewModel _viewModel;

    models = ((SampleController<UserProfile>)ViewContext.Controller).DataSource;
    _modelState = (ModelStateDictionary)ViewBag.ModelState;
    _model = (UserProfile)this.Model;
    _viewModel = (LessonTimeBookingViewModel)ViewBag.ViewModel;

    DateTime eventDate = _viewModel.ClassTimeStart ?? DateTime.Today;

}

@section LearnerBirthInfo
{
    @{
        Html.RenderPartial("~/Views/ConsoleHome/Index/Coach/Part_1/LearnerBirthday.cshtml");
    }
}

@section LessonInfo
{
    @{
        Html.RenderPartial("~/Views/ConsoleHome/Index/Coach/Part_1/LessonInfo.cshtml");
    }
}

@section ContractInfo
{
    @{
        Html.RenderPartial("~/Views/ConsoleHome/Index/Coach/Part_1/ContractInfo.cshtml");
    }
}

